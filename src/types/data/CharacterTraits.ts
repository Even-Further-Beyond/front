import { Color, HairLength, AgeGroup, Gender } from './';

interface CharacterTraits {
  ageGroups: AgeGroup[];
  genders: Gender[];
  colors: Color[];
  hairLengths: HairLength[];
}

export default CharacterTraits;
